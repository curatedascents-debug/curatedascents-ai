import { test as authTest, expect } from './auth.fixture';

/**
 * Base test fixture that extends the auth fixture.
 * Add any additional shared fixtures here.
 */
export const test = authTest;
export { expect };
